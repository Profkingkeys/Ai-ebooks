{
  "name": "practical-ai-library",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "32 practical AI field guides with an offline reader and reproducible labs.",
  "scripts": {
    "test": "node scripts/validate-book.js",
    "test:labs": "python3 -m unittest discover -s labs/data-analysis -p 'test_*.py'",
    "build": "node scripts/build-reader.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "marked": "18.0.13"
  }
}
